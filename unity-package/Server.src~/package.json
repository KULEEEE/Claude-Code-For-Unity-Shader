{
  "name": "unity-agent-headless",
  "version": "0.12.0",
  "private": true,
  "description": "Headless Claude runner + internal image-gen MCP bundled into unity-package/Server~",
  "type": "module",
  "scripts": {
    "build": "tsc && node esbuild.config.mjs",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.1",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.3.3"
  }
}
