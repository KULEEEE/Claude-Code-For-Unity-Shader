---
name: shader-list
description: List all shaders in the Unity project
---

List all shaders in the current Unity project using `list_shaders`.

If the user provided a filter keyword, pass it as the `filter` parameter.
Display the results as a formatted table with shader name and path.
